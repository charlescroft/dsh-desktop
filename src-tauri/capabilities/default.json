{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window (splash page IPC)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
