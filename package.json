{
  "name": "dsh-desktop",
  "version": "0.2.2",
  "private": true,
  "description": "Standalone Tauri desktop shell for the DeepSeek Harness (dsh) web profile",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "fetch:node": "node scripts/fetch-node.mjs",
    "icon": "node scripts/make-icon.mjs && tauri icon scripts/app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0",
    "sharp": "^0.34.0"
  }
}
